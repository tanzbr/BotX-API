const config = {
  app: {
    port: 8080,
    // API SENDO USADA ATUALMENTE (DISPONIVEIS: asaas)
    using_api: "asaas",
  },
  api_asaas: {
    token:
      "***REMOVED***",
  },
};

module.exports = config;
